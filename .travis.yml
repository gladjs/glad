sudo: false
language: node_js
cache:
  directories:
    - node_modules
before_script:
  - npm prune

node_js:
  - "node"

services:
  - redis-server

after_success:
  - npm run coveralls
script:
  cd tests/mock-app && npm install
